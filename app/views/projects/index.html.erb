<h1>Projects</h1>

<% @projects.each do |project| %>
  <div class="record">
    <p><%# project.id %><%= link_to project.title, project_path(project) %></p>
    <p><%= project.description %></p>
  </div>

  <br />
<% end %>

<h2><%= link_to "New Project", new_project_path %></h2>